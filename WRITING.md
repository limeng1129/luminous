# 怎么用这个站

## 写一篇文章

打开 `你的网址/compose`，或点导航栏最右边的「写」。

- **分类**：旅行 / 感悟 / 生活
- **标题**、**导语**（标题下那一句）、**地点·时间**
- **封面照片**：会用在首页大图和文章顶部，同时自动进入影像区
- **正文**：支持 Markdown

正文里能用的语法：

```
## 小标题
### 更小的标题

**加粗**   *斜体*   `代码`

> 引用会排成居中的大字，是文章里的重音

- 列表项
- 列表项

![图片说明](图片链接)

---
```

单独一行的图片会**出血成大图**，说明文字显示在图下方。
想插图不用手写链接：正文下面有「插入照片到正文」，选一张图会自动把语句填到光标处。

## 给站点上锁（重要）

不设密码的话，**任何人打开你的网址都能发文章、删照片**。

在 Render → Environment 加一条：

| Key | Value |
|---|---|
| `ADMIN_PASSWORD` | 你自己设的密码 |

保存后自动重新部署。之后：

- 所有人都能正常浏览、阅读、看照片
- 但发布和删除需要先在 `/admin/login` 输密码
- 登录状态保存 60 天；改密码会让所有旧登录立即失效

本地开发时不设这个变量，就不会拦你。

## 看高清照片

影像区点任意一张进入全屏：

- **再点一次**放大到 2.4 倍，可以拖动查看细节
- **滚轮**可无级缩放（最高 5 倍）
- **左右方向键**切换，**Esc** 退出，按 **0** 复位
- 手机上左右滑动切换，下滑关闭

## 页面一览

| 地址 | 内容 |
|---|---|
| `/` | 首页：最新一篇做大图，下面是文章列表和影像区 |
| `/writing` | 全部随笔，可按分类筛选 |
| `/p/<slug>` | 文章阅读页 |
| `/gallery` | 全部照片 |
| `/about` | 关于 |
| `/compose` | 写作（需登录） |
| `/health/storage` | 运行状态自检 |

---

## 光谱

`/spectrum`

每张照片被压成一道颜色——提取主色、算出明度和冷暖——然后铺成一条横贯屏幕的光带。

- **横着划过光带**，照片一张张浮出来
- **点一下**进入全屏看原图
- **切换排法**，色条会重新排列：
  - **时间** —— 从最早到最近，这条光带就是你这些年的颜色年表
  - **冷暖** —— 从最冷的青灰，一路烧到最暖的橘红
  - **明暗** —— 从最暗排到最亮
- 键盘左右键可以逐张移动，回车放大

**关于老照片**：颜色是在上传时分析的。如果你在加这个功能之前就上传过照片，
它们会显示成灰色——登录后到光谱页点「现在分析」补算即可（一次处理 40 张，
照片多的话多点几次）。

---

## 地图

`/map`

走过的地方连成的形状。不接任何地图服务——内置了 219 个常见地名的坐标，
从文章的「地点」和照片说明里认出城市名，直接画成光点。

- **点的大小**反映那里有多少文章和照片
- **虚线**按第一次去的时间把地点连起来，就是你的路线
- **点一下**任意光点，右侧显示那里的文章和照片
- 淡色的是**参照城市**，帮你判断自己大概在世界的哪一块
- 视野会自动缩放：只去过国内就自动放大到国内

**怎么让地点被认出来**：写文章时在「地点·时间」里写上城市名，
比如 `大理 · 去年秋天`、`东京 · 傍晚`。写「家」「厨房」这种不会上图，这是正常的。

**地名不在表里**：地图下方会列出没认出来的地名。想加的话，
打开 `places.py`，往 `PLACES` 里补一行就行：

```python
"你的地名": (纬度, 经度),
```

---

## 此刻

`/hour`，首页上也有一条安静的横带。

一天二十四小时，每一格是那个钟点的平均颜色，高度是照片数量。
它会在夜里变暗、正午发白、黄昏转暖——因为光本来就是这样的。
整个页面还会染上当前钟点的光。

- 打开时自动跳到**你本地的此刻**，跨过整点会自己跟上
- **点任意一格**看那个钟点的照片，页面颜色跟着换
- 左右方向键可以逐个钟点走
- 首页那条横带会说一句「现在是 18:20，黄昏。这个钟点你拍过 4 张。」
  这个钟点没有照片的话，它会自动找最近的一个有照片的时辰

### 照片的拍摄时辰是怎么来的

按可靠程度依次尝试，**认不出就留空，不猜**：

1. **EXIF 拍摄时间** —— 手机和相机照片自带，最准。传自己拍的照片基本都能认出来
2. **说明里的时间词** —— 「清晨」「傍晚」「深夜」「午后」，也认「六点」「18:30」
3. **都没有就不放进来** —— 不用亮度去蒙，一张中午的室内暗照会被误判成夜里，那是假数据

页面底部会告诉你有多少张没认出时辰。想让它们上榜，在照片说明里写上时间词即可。
